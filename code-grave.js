//DO I NEED THIS CODE BELOW, I DO NOT KNOW  THE DIFFERENCE BETTWEEN THIS CODE AND THE OTHERS BELOW

//target all the ID selectors from the HTML in JavaScript
  //function fetchTasks() {
    //$.ajax({
     // url: `${API_URL}/tasks`,
     // method: 'GET',
     // dataType: 'json',
     /// success: function (data) {
        //tasks = data;
        //createTasks();
      //},
    //});
 // }
  //function addTask(task) {
    //$.ajax({
      //url: `${API_URL}/tasks`,
      //method: 'POST',
      //contentType: 'application/json',
      //data: JSON.stringify(task),
      //success: function () {
        //fetchTasks();
      //},
   // });
 // }

  //function deleteTask(e, index) {
    //$.ajax({
     // url: `${API_URL}/tasks/${tasks[index].id}`,
      //method: 'DELETE',
      //success: function () {
        //tasks.splice(index, 1);
        //createTasks();
      //},
   // });
  //}

  //fetchTasks();

  //$('#form').submit(function (e) {
    //e.preventDefault();
    //formValidation();
  //});

 // function formValidation() {
    // Existing validation code...

    //acceptData();
  //}

  //function acceptData() {
    //const task = {
      //text: textInput.value,
      //date: dateInput.value,
      //description: textarea.value,
   //};

   // addTask(task);
    //resetForm();
    //$('#add').attr('data-bs-dismiss', 'modal');
    //$('#add').click();
  //}

  //function resetForm() {
    // Reset form fields...
  //}

  // Other functions (editTask, editPost, etc.) remain unchanged...
///Do I need the above code?
//fetch(' http://localhost:3000/posts', {
   //method: 'GET',
 //})
// .then(response => response.json())
// .then(data => console.log(data))
// .catch((error) => console.error('Error:', error));

 ///fetch(' http://localhost:3000/comments', {
  // method: 'GET',
 //})
 //.then(response => response.json())
 //.then(data => console.log(data))
 //.catch((error) => console.error('Error:', error));

 //fetch(' http://localhost:3000/profile', {
  // method: 'GET',
 //})
 //.then(response => response.json())
 //.then(data => console.log(data))
 //.catch((error) => console.error('Error:', error));

